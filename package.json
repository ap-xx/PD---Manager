{
  "name": "pd-manager",
  "private": true,
  "version": "1.0.0",
  "description": "Gerenciador de estoque para o jogo Project Delta (Roblox)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "generate-items": "node scripts/generate-items.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
